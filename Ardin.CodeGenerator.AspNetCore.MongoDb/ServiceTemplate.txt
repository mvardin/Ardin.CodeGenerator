using @ArdinNamespace.Data;
using Microsoft.Extensions.Options;
using MongoDB.Driver;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;

namespace @ArdinNamespace.Service
{
    public class @ArdinModelService
    {
        public readonly IMongoCollection<@ArdinModel> Collection;
        public @ArdinModelService(IOptions<AppSetting> config)
        {
            var mongoClient = new MongoClient(config.Value.ConnectionString);
            var mongoDatabase = mongoClient.GetDatabase(config.Value.DatabaseName);
            Collection = mongoDatabase.GetCollection<@ArdinModel>("@ArdinModel");
        }
    }
}